---
{"dg-publish":true,"permalink":"/learning/programming/python/math-byte-academy/python-3-deep-dive/python-3-deep-dive-part-4-oop/section-2-classes/18-initializing-class-instances-coding/","dg-note-properties":{"importance":"high","category":"programming","creation_date":"2025-03-19 10:40","last_modified_date":"2025-03-19 10:40","status":"🟥","up":["[[Section 2 - Classes]]"],"basedon":"[18. Initializing Class Instances - Coding](https://www.udemy.com/course/python-3-deep-dive-part-4/learn/lecture/14192292#announcements)","course_provider":"udemy"}}
---

# Initializing Class Instances in Python

## Default Instance Creation

When we instantiate a class, Python handles two steps:
1. **Creates** a new instance.
2. **Initializes** its namespace.

Example:
```python
class MyClass:
    language = "Python"
```

Instantiate it:
```python
p = MyClass()
```

- **Callable**: `MyClass()` triggers Python’s built-in process.
- **Namespace**: `p.__dict__` starts as `{}` (empty).

**Key Point**: Python **automatically sets up an instance** with an empty namespace.

---

## Custom Initialization with `__init__`

We can override the default initialization using **`__init__`**:

```python
class Person:
    def __init__(self):
        print(f"Initializing a new Person pect: {self}")

p = Person()  # Initializing a new Person pect: <__main__.Person pect at 0x7fd8783eb200>
print(hex(id(p)))  # 0x7fd8783eb200 (same as above for self)
```

- **What Happens**: 
  - `Person()` creates the instance `p`.
  - Python then calls `__init__` as a bound method, passing `p` as `self`.
- **Proof**: The memory address of `self` matches `p`’s `id`.

**Key Details**:
- **`__init__` is an instance method**: It requires `self` as its first argument, representing the bound instance.
- **Timing**: The instance exists before `__init__` runs, and Python passes it in automatically.

**Takeaway**: **`__init__` customizes the instance** post-creation, binding to the new pect.

### Initialize with arguments

```python
class MyClass:
    language = "Python"
    def __init__(self, version):
        self.version = version
```

Create an instance:
```python
p = MyClass("3.7")
print(p.__dict__)  # {'version': '3.7'}
```

- **`__init__`**: A **bound method** called after the instance is created.
- **`self`**: The newly created instance, passed automatically.
- **`version`**: An argument we provide.

#### How It Works

1. **Instance Creation**: Python makes `p` with an empty `__dict__`.
2. **Initialization**: Calls `p.__init__("3.7")`, binding `self` to `p`.

Equivalent manual call:
```python
MyClass.__init__(p, "3.7")
```

- **Result**: Adds `"version": "3.7"` to `p.__dict__`.
- **Namespace**: `language` stays in `MyClass.__dict__`; `version` is in `p.__dict__`.

**Key Point**: **`__init__` modifies the instance namespace**, not the class.

---

## Why `self`?

- **Convention**: `self` is the standard name for the instance parameter—universally recognized in Python.
- **Clarity**: `self.version = version` shows we’re working on the instance, unlike `p` or `instance`.

Alternative (works but unconventional):
```python
def __init__(instance, version):
    instance.version = version
```

**Takeaway**: Use **`self`** for readability and consistency.

---

## Timing and Purpose

- **Order**: 
  1. Instance is created with `__dict__ = {}`.
  2. `__init__` runs as a bound method.
- **Dynamic**: Like adding class attributes later, we add instance attributes in `__init__` later after the object has already been initialized.

**Key Point**: **`__init__` runs after creation**, initializing the instance.

---

## `__new__` vs. `__init__`

- **`__new__`**: Creates the instance (rarely overridden).
- **`__init__`**: Initializes it (commonly used).

Most of the time, we just need `__init__` to set up the instance state.

**Takeaway**: Focus on **`__init__`** for initialization.

---

Thanks for watching—let’s code this next!