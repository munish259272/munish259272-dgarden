---
{"dg-publish":true,"permalink":"/learning/programming/python/math-byte-academy/python-3-deep-dive/python-3-deep-dive-part-4-oop/section-2-classes/28-deleting-properties-coding/","dg-note-properties":{"importance":"high","category":"programming","creation_date":"2025-03-19 10:40","last_modified_date":"2025-03-19 10:40","status":"🟥","up":["[[Section 2 - Classes]]"],"basedon":"[28. Deleting Properties - Coding](https://www.udemy.com/course/python-3-deep-dive-part-4/learn/lecture/14192318#overview)","course_provider":"udemy"}}
---


# Deleting Properties: A Quick Guide

## Property Deleters Explained

Deleting a property doesn’t remove it from the class—it triggers a custom **deleter method** on the instance. Here’s how:

```python
class Person:
    def __init__(self, name):
        self.name = name  # Looks bare, but uses setter
    
    def get_name(self):
        print("Getter")
        return self._name
    
    def set_name(self, value):
        print("Setter")
        self._name = value
    
    def del_name(self):
        print("Deleter")
        del self._name
    
    name = property(fget=get_name, fset=set_name, fdel=del_name, doc="Person name")

p = Person("Guido")
print(p.__dict__)  # {'_name': 'Guido'}
del p.name         # Deleter, removes _name
print(p.__dict__)  # {}
p.name = "Alex"    # Setter, adds _name back
print(p.__dict__)  # {'_name': 'Alex'}
```

- **`del p.name`**: Runs `del_name`, clearing `_name` from the instance.
- **Class Intact**: `Person.name` (the property) remains.

**Key Point**: **Deleters act on instance data**, not the property itself.

---

## Using Decorators

The same setup with decorators is cleaner:

```python
class Person:
    def __init__(self, name):
        self.name = name
    
    @property
    def name(self):
        print("Getter")
        return self._name
    
    @name.setter
    def name(self, value):
        print("Setter")
        self._name = value
    
    @name.deleter
    def name(self):
        print("Deleter")
        del self._name

p = Person("Alex")
print(p.__dict__)  # {'_name': 'Alex'}
del p.name         # Deleter
print(p.__dict__)  # {}
p.name = "Guido"   # Setter
print(p.__dict__)  # {'_name': 'Guido'}
```

- **Syntax**: `@property`, `@name.setter`, `@name.deleter`—same name ties them together.
- **Docstring**: Add it under `@property` for `help(Person.name)`.

**Takeaway**: **Decorators streamline deleter setup**.

---

## Flexibility and Use Case

- **Options**: Use `del p.name` or `delattr(p, "name")`—both call the deleter.
- **Purpose**: Rare, but handy for cleanup (e.g., removing instance data).

**Key Point**: **Deleters are optional but powerful** when needed.

---

Thanks for watching—see you next time!
