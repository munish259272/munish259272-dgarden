---
{"dg-publish":true,"permalink":"/learning/programming/c-and-t/learnbay/statistics-and-machine-learning/33-class-64-apr-20-2024-sat-machine-learning/","dg-note-properties":{}}
---


Dhruv - 9538206789
Resource : [Site Unreachable](https://drive.google.com/drive/folders/1qgVtB3VLnC_nxpCoRQaZE1cjUE3Bbfgx)
find the files that i created for this class here `/home/munish259272/ExtraSoftware/learnbay files`
## <span style="color:#000000;font-size:1.5em"><i>---Continued from last class ---&gt</i></span>

### metadata
```run-python {pre}
from pathlib import Path
present_working_directory = Path(@vault_path) / Path(@note_path).parent
current_file_name = @title
video_note = f"{present_working_directory}/{current_file_name}.mp4"

import subprocess
import argparse

def play_video(start, end=None):

    # Construct mpv command
    command = [
        "mpv",
        "--start=" + start,
        "--geometry=1000x500+1920+0",
        video_note
    ]
    
    if end is not None:
	    command.append("--end=" + end)
 
    # Run mpv command
    try:
        subprocess.run(command, stderr=subprocess.DEVNULL, check=True)
    except subprocess.CalledProcessError as e:
        print("Error:", e)
        return
# play_video("00:00:00", "00:00:09")
# play_video("00:00:00")
```

```run-python
play_video("00:00:00")
```

# First script
[[DT_Iteration7-Copy1.ipynb]]
```run-python
play_video("00:00:00")
```
### At --start=00:00:00  (--end=00:16:43 Some good questions , can be watched)
```run-python
play_video("00:00:00", "00:16:43")
```
I have spitted my data but when we talk about regression there is no purity or impurity here then how will you choose the variable for splitting?
Answer : The split will happen based on the variable which will reduce the MSE/variance most 

### At 00:18:07 implementation of decision tree and random forest
```run-python
play_video("00:18:07")
```

>[!info]- The classification goal is to predict if the client will subscribe (yes/no) a term deposit (variable y).
> 
> Attribute Information:
> 
> **<span style="font-weight:bold; color:#000000">Data</span>**
> **<span style="font-weight:bold; font-weight:bold; color:#000000">bank client data:</span>**
> 1 - age (numeric)
> 2 - job : type of job (categorical: 'admin.','blue-collar','entrepreneur','housemaid','management','retired','self-employed','services','student','technician','unemployed','unknown')
> 3 - marital : marital status (categorical: 'divorced','married','single','unknown'; note: 'divorced' means divorced or widowed)
> 4 - education (categorical: 'basic.4y','basic.6y','basic.9y','high.school','illiterate','professional.course','university.degree','unknown')
> 5 - default: has credit in default? (categorical: 'no','yes','unknown')
> 6 - housing: has housing loan? (categorical: 'no','yes','unknown')
> 7 - loan: has personal loan? (categorical: 'no','yes','unknown')
> **<span style="font-weight:bold; color:#000000">related with the last contact of the current campaign:</span>**
> 8 - contact: contact communication type (categorical: 'cellular','telephone') 
> 9 - month: last contact month of year (categorical: 'jan', 'feb', 'mar', ..., 'nov', 'dec')
> 10 - day_of_week: last contact day of the week (categorical: 'mon','tue','wed','thu','fri')
> 11 - duration: last contact duration, in seconds (numeric). Important note: this attribute highly affects the output target (e.g., if duration=0 then y='no'). Yet, the duration is not known before a call is performed. Also, after the end of the call y is obviously known. Thus, this input should only be included for benchmark purposes and should be discarded if the intention is to have a realistic predictive model.
> 12 - campaign: number of contacts performed during this campaign and for this client (numeric, includes last contact)
> 13 - pdays: number of days that passed by after the client was last contacted from a previous campaign (numeric; 999 means client was not previously contacted)
> 14 - previous: number of contacts performed before this campaign and for this client (numeric)
> 15 - poutcome: outcome of the previous marketing campaign (categorical: 'failure','nonexistent','success')
> 
> **<span style="font-weight:bold; color:#000000">Output variable (desired target):</span>**
> 21 - y - has the client subscribed a term deposit? (binary: 'yes','no')

```run-python
import pandas as pd
import numpy as np
from sklearn import preprocessing
from sklearn.model_selection import train_test_split
from sklearn.tree import DecisionTreeClassifier
from sklearn.metrics import confusion_matrix
from sklearn.metrics import accuracy_score
from sklearn.metrics import roc_curve, auc
import matplotlib.pyplot as plt

# from sklearn.externals.six import StringIO  
from IPython.display import Image         # To show the tree based structure
from sklearn.tree import export_graphviz  # To show the tree
import pydotplus                          # To show the tree
import seaborn as sns
```

```run-python
df = pd.read_csv('/home/munish259272/ExtraSoftware/Programming/Programming/Courses and Tutorials/Learnbay/Statistics & Machine Learning/course files/Machine Learning with Dhruv- 16th March 2024-20240410T173353Z-001/Machine Learning with Dhruv- 16th March 2024/5.Decision Tree/Code+Dataset/bank.csv')
```

```run-python
temp = df.columns.values[0]
print(temp)
```

```run-python
df.head()
```

|     | age | job        | marital | education | default | balance | housing | loan | contact | day | month | duration | campaign | pdays | previous | poutcome | deposit |
| --- | --- | ---------- | ------- | --------- | ------- | ------- | ------- | ---- | ------- | --- | ----- | -------- | -------- | ----- | -------- | -------- | ------- |
| 0   | 59  | admin.     | married | secondary | no      | 2343    | yes     | no   | unknown | 5   | may   | 1042     | 1        | -1    | 0        | unknown  | yes     |
| 1   | 56  | admin.     | married | secondary | no      | 45      | no      | no   | unknown | 5   | may   | 1467     | 1        | -1    | 0        | unknown  | yes     |
| 2   | 41  | technician | married | secondary | no      | 1270    | yes     | no   | unknown | 5   | may   | 1389     | 1        | -1    | 0        | unknown  | yes     |
| 3   | 55  | services   | married | secondary | no      | 2476    | yes     | no   | unknown | 5   | may   | 579      | 1        | -1    | 0        | unknown  | yes     |
| 4   | 54  | admin.     | married | tertiary  | no      | 184     | no      | no   | unknown | 5   | may   | 673      | 2        | -1    | 0        | unknown  | yes     |

#### <span style="color:#000000">Checking null values</span>
```run-python
df.isnull().sum()
```

```run-python
corrmat = df.corr()
k = 10 #number of variables for heatmap
cols = corrmat.nlargest(k, 'age')['age'].index
cm = np.corrcoef(df[cols].values.T)
sns.set(font_scale=1.25)
hm = sns.heatmap(cm, cbar=True, annot=True, square=True, fmt='.2f', annot_kws={'size': 10}, yticklabels=cols.values, xticklabels=cols.values)
plt.show()
```

### Don't do like this to check outliers. i.e one by one checking all the variables
#### <span style="color:#000000">Checking outliers</span>
```run-python
plt.figure(figsize=(10,6))
df.balance.plot(kind='box')
plt.show()
```

```run-python
df['balance'].describe()
```

```run-python
df.drop(df[df['balance']>1708].index,axis=0,inplace=True)
df.drop(df[df['balance']<122].index,axis=0,inplace=True)
```

```run-python
plt.figure(figsize=(10,6))
df.balance.plot(kind='box')
plt.show()
```


```run-python
plt.figure(figsize=(10,6))
df.age.plot(kind='box')
plt.show()
```

```run-python
df['age'].describe()
```

```run-python
df.drop(df[df['age']>48].index,axis=0,inplace=True)
df.drop(df[df['age']<32].index,axis=0,inplace=True)
```

```run-python
plt.figure(figsize=(10,6))
df.age.plot(kind='box')
plt.show()
```


```run-python
plt.figure(figsize=(10,6))
df.day.plot(kind='box')
plt.show()
```

```run-python
df['day'].describe()
```

```run-python
df.drop(df[df['day']>19].index,axis=0,inplace=True)
df.drop(df[df['day']<12].index,axis=0,inplace=True)
```

```run-python
plt.figure(figsize=(10,6))
df.day.plot(kind='box')
plt.show()
```

```run-python
plt.figure(figsize=(10,6))
df.duration.plot(kind='box')
plt.show()
```

```run-python
df['duration'].describe()
```

```run-python
df.drop(df[df['duration']>190].index,axis=0,inplace=True)
df.drop(df[df['duration']<106].index,axis=0,inplace=True)
```

```run-python
plt.figure(figsize=(10,6))
df.duration.plot(kind='box')
plt.show()
```

```run-python
plt.figure(figsize=(10,6))
df.campaign.plot(kind='box')
plt.show()
```

```run-python
df['campaign'].describe()
```

```run-python
df.drop(df[df['campaign']>3].index,axis=0,inplace=True)
df.drop(df[df['campaign']<1].index,axis=0,inplace=True)
```

```run-python
plt.figure(figsize=(10,6))
df.campaign.plot(kind='box')
plt.show()
```

```run-python
plt.figure(figsize=(10,6))
df.pdays.plot(kind='box')
plt.show()
```

```run-python
df['pdays'].describe()
```

```run-python
df.drop(df[df['pdays']>92].index,axis=0,inplace=True)
df.drop(df[df['pdays']<-1].index,axis=0,inplace=True)
```

```run-python
plt.figure(figsize=(10,6))
df.pdays.plot(kind='box')
plt.show()
```

```run-python
plt.figure(figsize=(10,6))
df.previous.plot(kind='box')
plt.show()
```

```run-python
df['previous'].describe()
```

```run-python
df.drop(df[df['previous']>0].index,axis=0,inplace=True)
```

```run-python
plt.figure(figsize=(10,6))
df.previous.plot(kind='box')
plt.show()
```

### Data preprocessing
```run-python
# This function can be further shorten to few lines by creating a list of categorical vars and then looping through it.
def preprocessor(df):
    res_df = df.copy()
    le = preprocessing.LabelEncoder()
    
    res_df['job'] = le.fit_transform(res_df['job'])
    res_df['marital'] = le.fit_transform(res_df['marital'])
    res_df['education'] = le.fit_transform(res_df['education'])
    res_df['default'] = le.fit_transform(res_df['default'])
    res_df['housing'] = le.fit_transform(res_df['housing'])
    res_df['loan'] = le.fit_transform(res_df['loan'])
    res_df['contact'] = le.fit_transform(res_df['contact'])
    res_df['month'] = le.fit_transform(res_df['month'])
    res_df['poutcome'] = le.fit_transform(res_df['poutcome'])
    res_df['deposit'] = le.fit_transform(res_df['deposit'])
    return res_df
```

```run-python
encoded_df = preprocessor(df)
print(encoded_df.head(2))
```

```run-python
x = encoded_df.drop('deposit',axis=1).values
y = encoded_df['deposit'].values
```

### Split Data into Train and Test data sets
```run-python
x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=0.2)
```

## Build Decision Tree Model
### Decision Tree with Cart(Gini)
###### Decision Tree with depth 2
```run-python
model_dt_2 = DecisionTreeClassifier(random_state=1,max_depth=2)
model_dt_2.fit(x_train,y_train)

model_dt_2_score_tarin = model_dt_2.score(x_train,y_train)
model_dt_2_score_test = model_dt_2.score(x_test,y_test)

print('Trining Score',model_dt_2_score_tarin)
print('Testing Score',model_dt_2_score_test)
```
#Assignment: Try with depth = 3

###### Decision Tree with depth 4
```run-python
model_dt_4 = DecisionTreeClassifier(random_state=1,max_depth=4)
model_dt_4.fit(x_train,y_train)

model_dt_4_score_tarin = model_dt_4.score(x_train,y_train)
model_dt_4_score_test = model_dt_4.score(x_test,y_test)

print('Trining Score',model_dt_4_score_tarin)
print('Testing Score',model_dt_4_score_test)
```

###### Decision Tree with depth 6
```run-python
model_dt_6 = DecisionTreeClassifier(random_state=1,max_depth=6)
model_dt_6.fit(x_train,y_train)

model_dt_6_score_tarin = model_dt_6.score(x_train,y_train)
model_dt_6_score_test = model_dt_6.score(x_test,y_test)

print('Trining Score',model_dt_6_score_tarin)
print('Testing Score',model_dt_6_score_test)
```

###### Decision Tree with depth 6
```run-python
model_dt_8 = DecisionTreeClassifier(random_state=1,max_depth=8)
model_dt_8.fit(x_train,y_train)

model_dt_8_score_tarin = model_dt_8.score(x_train,y_train)
model_dt_8_score_test = model_dt_8.score(x_test,y_test)

print('Trining Score',model_dt_8_score_tarin)
print('Testing Score',model_dt_8_score_test)
```

### Decision Tree with c4.5(entropy)
```run-python
model_dt = DecisionTreeClassifier(max_depth=2,criterion='entropy')

model_dt.fit(x_train,y_train)

y_pred_dt = model_dt.predict_proba(x_test)[:,1]
# y_pred_dt

model_dt_score_train = model_dt.score(x_train,y_train)
print(model_dt_score_train)

model_dt_score_test = model_dt.score(x_test,y_test)
print(model_dt_score_test)
```

## Graphical Represetation of Tree
```run-python
# pip install graphviz
# conda install graphviz
from io import StringIO
```

```run-python
plt.figure(figsize=(10,8))
dot_data = StringIO()
export_graphviz(model_dt_8,out_file=dot_data,
               filled=True,rounded=True,
               special_characters=True)
graph = pydotplus.graph_from_dot_data(dot_data.getvalue())
Image(graph.create_png())
```

## Performance Matrics
```run-python
fpr_dt,tpr_dt,_=roc_curve(y_test,y_pred_dt)
roc_auc_dt = auc(fpr_dt,tpr_dt)
```

```run-python
predictions = model_dt_6.predict(x_test)
# print(predictions)

print(model_dt_6.score(x_test,y_test))
y_actual_result = y_test[0]
# print(y_actual_result)
```

```run-python
print(predictions)
```

```run-python
df_preditor_without_target = encoded_df.drop('deposit',axis=1).values
df_preditor_only_target = encoded_df['deposit'].values
```

```run-python
print(df_preditor_only_target)
```

```run-python
predictions1 = model_dt_6.predict(df_preditor_without_target)
```

```run-python
print(predictions1)
```

```run-python
encoded_df['test_data'] = np.where(df_preditor_only_target,predictions1,df_preditor_only_target)
```

```run-python
df['test_data'] = np.where(df_preditor_only_target,predictions1,df_preditor_only_target)
```

```run-python
encoded_df.head(3)
```

```run-python
df.head(3)
```

```run-python
df['test_data'].replace(to_replace=1,value='True',inplace=True)
```

```run-python
df['test_data'].replace(to_replace=0,value='False',inplace=True)
```

```run-python
df.to_csv('Bank_with_Prediction_3_Point_Summary.csv')
```

## Area Under the Curve
```run-python
plt.figure(1)
lw=2
plt.plot(fpr_dt,tpr_dt,color='orange',lw=lw,label='Decision Tree(AUC = %0.2f)'%roc_auc_dt)
plt.plot([0,1],[0,1],color='blue',lw=lw,linestyle='--')

plt.xlim([0.0,1.0])
plt.ylim([0.0,1.05])
plt.xlabel("False Positive Rate")
plt.ylabel("True Positive Rate")
plt.title("area Under the Curve")
# plt.legend(loc="upper left")
plt.legend(loc="lower right")
```

## Confusion Matrix
```run-python
print(confusion_matrix(y_test,predictions))
```

```run-python
# TP = 2, TN = 30, FP = 6,FN = 1 ( The position of TP, TN, FP and FN is fixed for Conf Matrix plotted using sklearn default)
"""
(IN matrix form:)

1,1 = TN
1,2 = FP
2,1 = FN
2,2 = TP

"""
```

```run-python
accuracy_score(y_test,predictions)
```

```run-python
def create_conf_mat(y_test, predictions):
    """Function returns confusion matrix comparing two arrays"""
    if (len(y_test.shape) != len(predictions.shape) == 1):
        return print('Arrays entered are not 1-D.\nPlease enter the correctly sized sets.')
    elif (y_test.shape != predictions.shape):
        return print('Number of values inside the Arrays are not equal to each other.\nPlease make sure the array has the same number of instances.')
    else:
        # Set Metrics
        test_crosstb_comp = pd.crosstab(index = y_test,
                                        columns = predictions)
        # Changed for Future deprecation of as_matrix
        test_crosstb = test_crosstb_comp.values
        return test_crosstb
```

```run-python
plt.figure(figsize=(10,8))
conf_mat = create_conf_mat(y_test, predictions)
sns.heatmap(conf_mat, annot=True, fmt='d', cbar=False)
plt.xlabel('Predicted Values')
plt.ylabel('Actual Values')
plt.title('Actual vs. Predicted Confusion Matrix')
plt.show()
```

#Assignments
```run-python
"""
Assignments:

# Use decisiontreeregressor to predict the insurance charges or premium. Use criteria = "Squared Error". 
and evaluate using R2, adj r2, MSE< MAE< RMSE etc.

# Use other hyperparameters in classifier. e.g. - min_sample_leaf, min_impurity_decrease, max_leaf_nodes etc 
(can use GridSearchCV)



"""
```

#Assignment
```run-python
#Assignment: Pls do the classification using Adaboost by importing it from sklearn

# Assuming that X_train, X_test, y_train and y_test is available and we have already have a base estimator = DecisionTree

from sklearn.ensemble import AdaBoostClassifier

#Base estimator (now it is renamed as estimator), by default if we do not provide the base estimator, then it will select DT with max_depth = 1.
base_classifier = DecisionTreeClassifier(max_depth = 1)

#Instanciate the classifier
adaboost_classifier = AdaBoostClassifier(base_estimator = base_classifier, n_estimators = 10, random_state = 37)

#Fit the data
adaboost_classifier.fit(x_train, y_train)

#Predict (Training)

ada_train_pred = adaboost_classifier.predict(x_train)


#Predict (Test)

ada_test_pred = adaboost_classifier.predict(x_test)


#Findout accuracy, precision, recall, f1-score etc.
```

```run-python
ada_train_accuracy = accuracy_score(y_train,ada_train_pred)
print("adaboost training accuracy: ", ada_train_accuracy)
```

```run-python
ada_test_accuracy = accuracy_score(y_test,ada_test_pred)
ada_test_accuracy
```

# At 01:22:09 Another python script
[[RandomForest_Classifier.ipynb]]
```run-python
# Import modules
%matplotlib inline

import time
import random
import numpy as np
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt
from sklearn.metrics import roc_curve, auc
from sklearn.metrics import confusion_matrix
from sklearn.metrics import classification_report
from sklearn.model_selection import KFold, cross_val_score
from sklearn.model_selection import train_test_split, GridSearchCV
from sklearn.ensemble import RandomForestClassifier 
from urllib.request import urlopen 

plt.style.use('ggplot')
pd.set_option('display.max_columns', 500) 
```

```run-python
breast_cancer = pd.read_csv('data.csv')

names = ['id', 'diagnosis', 'radius_mean', 
         'texture_mean', 'perimeter_mean', 'area_mean', 
         'smoothness_mean', 'compactness_mean', 
         'concavity_mean','concave_points_mean', 
         'symmetry_mean', 'fractal_dimension_mean',
         'radius_se', 'texture_se', 'perimeter_se', 
         'area_se', 'smoothness_se', 'compactness_se', 
         'concavity_se', 'concave_points_se', 
         'symmetry_se', 'fractal_dimension_se', 
         'radius_worst', 'texture_worst', 
         'perimeter_worst', 'area_worst', 
         'smoothness_worst', 'compactness_worst', 
         'concavity_worst', 'concave_points_worst', 
         'symmetry_worst', 'fractal_dimension_worst'] 

dx = ['Benign', 'Malignant']
```

```run-python
breast_cancer.head()
```

|     | id       | diagnosis | radius_mean | texture_mean | perimeter_mean | area_mean | smoothness_mean | compactness_mean | concavity_mean | concave points_mean | symmetry_mean | fractal_dimension_mean | radius_se | texture_se | perimeter_se | area_se | smoothness_se | compactness_se | concavity_se | concave points_se | symmetry_se | fractal_dimension_se | radius_worst | texture_worst | perimeter_worst | area_worst | smoothness_worst | compactness_worst | concavity_worst | concave points_worst | symmetry_worst | fractal_dimension_worst | Unnamed: 32 |
| --- | -------- | --------- | ----------- | ------------ | -------------- | --------- | --------------- | ---------------- | -------------- | ------------------- | ------------- | ---------------------- | --------- | ---------- | ------------ | ------- | ------------- | -------------- | ------------ | ----------------- | ----------- | -------------------- | ------------ | ------------- | --------------- | ---------- | ---------------- | ----------------- | --------------- | -------------------- | -------------- | ----------------------- | ----------- |
| 0   | 842302   | M         | 17.99       | 10.38        | 122.80         | 1001.0    | 0.11840         | 0.27760          | 0.3001         | 0.14710             | 0.2419        | 0.07871                | 1.0950    | 0.9053     | 8.589        | 153.40  | 0.006399      | 0.04904        | 0.05373      | 0.01587           | 0.03003     | 0.006193             | 25.38        | 17.33         | 184.60          | 2019.0     | 0.1622           | 0.6656            | 0.7119          | 0.2654               | 0.4601         | 0.11890                 | NaN         |
| 1   | 842517   | M         | 20.57       | 17.77        | 132.90         | 1326.0    | 0.08474         | 0.07864          | 0.0869         | 0.07017             | 0.1812        | 0.05667                | 0.5435    | 0.7339     | 3.398        | 74.08   | 0.005225      | 0.01308        | 0.01860      | 0.01340           | 0.01389     | 0.003532             | 24.99        | 23.41         | 158.80          | 1956.0     | 0.1238           | 0.1866            | 0.2416          | 0.1860               | 0.2750         | 0.08902                 | NaN         |
| 2   | 84300903 | M         | 19.69       | 21.25        | 130.00         | 1203.0    | 0.10960         | 0.15990          | 0.1974         | 0.12790             | 0.2069        | 0.05999                | 0.7456    | 0.7869     | 4.585        | 94.03   | 0.006150      | 0.04006        | 0.03832      | 0.02058           | 0.02250     | 0.004571             | 23.57        | 25.53         | 152.50          | 1709.0     | 0.1444           | 0.4245            | 0.4504          | 0.2430               | 0.3613         | 0.08758                 | NaN         |
| 3   | 84348301 | M         | 11.42       | 20.38        | 77.58          | 386.1     | 0.14250         | 0.28390          | 0.2414         | 0.10520             | 0.2597        | 0.09744                | 0.4956    | 1.1560     | 3.445        | 27.23   | 0.009110      | 0.07458        | 0.05661      | 0.01867           | 0.05963     | 0.009208             | 14.91        | 26.50         | 98.87           | 567.7      | 0.2098           | 0.8663            | 0.6869          | 0.2575               | 0.6638         | 0.17300                 | NaN         |
| 4   | 84358402 | M         | 20.29       | 14.34        | 135.10         | 1297.0    | 0.10030         | 0.13280          | 0.1980         | 0.10430             | 0.1809        | 0.05883                | 0.7572    | 0.7813     | 5.438        | 94.44   | 0.011490      | 0.02461        | 0.05688      | 0.01885           | 0.01756     | 0.005115             | 22.54        | 16.67         | 152.20          | 1575.0     | 0.1374           | 0.2050            | 0.4000          | 0.1625               | 0.2364         | 0.07678                 | NaN         |
```run-python
# Setting 'id_number' as our index
breast_cancer.set_index(['id'], inplace = True) 
# Converted to binary
breast_cancer['diagnosis'] = breast_cancer['diagnosis'].map({'M':1, 'B':0})
```

## Missing Value checks and Data Cleaning
```run-python
breast_cancer.apply(lambda x: x.isnull().sum())
```

```run-python
# For later use in CART models
names_index = names[2:]
```

```run-python
del breast_cancer['Unnamed: 32']
```

```run-python
breast_cancer.head()
```

|          | diagnosis | radius_mean | texture_mean | perimeter_mean | area_mean | smoothness_mean | compactness_mean | concavity_mean | concave points_mean | symmetry_mean | fractal_dimension_mean | radius_se | texture_se | perimeter_se | area_se | smoothness_se | compactness_se | concavity_se | concave points_se | symmetry_se | fractal_dimension_se | radius_worst | texture_worst | perimeter_worst | area_worst | smoothness_worst | compactness_worst | concavity_worst | concave points_worst | symmetry_worst | fractal_dimension_worst |
| -------- | --------- | ----------- | ------------ | -------------- | --------- | --------------- | ---------------- | -------------- | ------------------- | ------------- | ---------------------- | --------- | ---------- | ------------ | ------- | ------------- | -------------- | ------------ | ----------------- | ----------- | -------------------- | ------------ | ------------- | --------------- | ---------- | ---------------- | ----------------- | --------------- | -------------------- | -------------- | ----------------------- |
| id       |           |             |              |                |           |                 |                  |                |                     |               |                        |           |            |              |         |               |                |              |                   |             |                      |              |               |                 |            |                  |                   |                 |                      |                |                         |
| ---      | ---       | ---         | ---          | ---            | ---       | ---             | ---              | ---            | ---                 | ---           | ---                    | ---       | ---        | ---          | ---     | ---           | ---            | ---          | ---               | ---         | ---                  | ---          | ---           | ---             | ---        | ---              | ---               | ---             | ---                  | ---            | ---                     |
| 842302   | 1         | 17.99       | 10.38        | 122.80         | 1001.0    | 0.11840         | 0.27760          | 0.3001         | 0.14710             | 0.2419        | 0.07871                | 1.0950    | 0.9053     | 8.589        | 153.40  | 0.006399      | 0.04904        | 0.05373      | 0.01587           | 0.03003     | 0.006193             | 25.38        | 17.33         | 184.60          | 2019.0     | 0.1622           | 0.6656            | 0.7119          | 0.2654               | 0.4601         | 0.11890                 |
| 842517   | 1         | 20.57       | 17.77        | 132.90         | 1326.0    | 0.08474         | 0.07864          | 0.0869         | 0.07017             | 0.1812        | 0.05667                | 0.5435    | 0.7339     | 3.398        | 74.08   | 0.005225      | 0.01308        | 0.01860      | 0.01340           | 0.01389     | 0.003532             | 24.99        | 23.41         | 158.80          | 1956.0     | 0.1238           | 0.1866            | 0.2416          | 0.1860               | 0.2750         | 0.08902                 |
| 84300903 | 1         | 19.69       | 21.25        | 130.00         | 1203.0    | 0.10960         | 0.15990          | 0.1974         | 0.12790             | 0.2069        | 0.05999                | 0.7456    | 0.7869     | 4.585        | 94.03   | 0.006150      | 0.04006        | 0.03832      | 0.02058           | 0.02250     | 0.004571             | 23.57        | 25.53         | 152.50          | 1709.0     | 0.1444           | 0.4245            | 0.4504          | 0.2430               | 0.3613         | 0.08758                 |
| 84348301 | 1         | 11.42       | 20.38        | 77.58          | 386.1     | 0.14250         | 0.28390          | 0.2414         | 0.10520             | 0.2597        | 0.09744                | 0.4956    | 1.1560     | 3.445        | 27.23   | 0.009110      | 0.07458        | 0.05661      | 0.01867           | 0.05963     | 0.009208             | 14.91        | 26.50         | 98.87           | 567.7      | 0.2098           | 0.8663            | 0.6869          | 0.2575               | 0.6638         | 0.17300                 |
| 84358402 | 1         | 20.29       | 14.34        | 135.10         | 1297.0    | 0.10030         | 0.13280          | 0.1980         | 0.10430             | 0.1809        | 0.05883                | 0.7572    | 0.7813     | 5.438        | 94.44   | 0.011490      | 0.02461        | 0.05688      | 0.01885           | 0.01756     | 0.005115             | 22.54        | 16.67         | 152.20          | 1575.0     | 0.1374           | 0.2050            | 0.4000          | 0.1625               | 0.2364         | 0.07678                 |
```run-python
print("Here's the dimensions of our data frame:\n", breast_cancer.shape)
print("Here's the data types of our columns:\n",breast_cancer.dtypes)
```

```run-python
breast_cancer.describe()
```

## Split the Dataset for Training & Testing
```run-python
feature_space = breast_cancer.iloc[:, breast_cancer.columns != 'diagnosis']
feature_class = breast_cancer.iloc[:, breast_cancer.columns == 'diagnosis']


training_set, test_set, class_set, test_class_set = train_test_split(feature_space,
                                                                    feature_class,
                                                                    test_size = 0.20, 
                                                                    random_state = 42)
```

```run-python
# Cleaning test sets to avoid future warning messages
class_set = class_set.values.ravel() 
test_class_set = test_class_set.values.ravel() 
```

## Random Forest Classifier
```run-python
# Set the random state for reproducibility
fit_rf = RandomForestClassifier(random_state=42)
```

## Hyper-Parameter Optimization using GridSearchCV
```run-python
np.random.seed(42)
start = time.time()

# removed auto from 'max_features': ['auto', 'sqrt', 'log2', None]. It looks like it is deprecated.

param_dist = {'max_depth': [2, 3, 4],
              'bootstrap': [True, False],
              'max_features': ['sqrt', 'log2', None],
              'criterion': ['gini', 'entropy']}

cv_rf = GridSearchCV(fit_rf, cv = 10,
                     param_grid=param_dist, 
                     n_jobs = 3)

cv_rf.fit(training_set, class_set)
print('Best Parameters using grid search: \n', cv_rf.best_params_)
end = time.time()
print('Time taken in grid search: {0: .2f}'.format(end - start))

## IMPORTANT NOTE : The second parameter of cv_rf.fit() expects a 1D array 
## so use y_train.values.ravel() instead of y_train or else the below eror will show.
# Error :
#    DataConversionWarning: A column-vector y was passed when a 1d array was expected. 
#    Please change the shape of y to (n_samples,), for example using ravel(). return 
#    fit_method(estimator, *args, **kwargs)

## Looks like max_feature 'auto' is deprecated and was causing problems so removed it
```
> [!important]- The second parameter of cv_rf.fit() expects a 1D array so use y_train.values.ravel() instead of y_train or else the below eror will show.
> Error :
> <span style="color:#FF0000">DataConversionWarning: A column-vector y was passed when a 1d array was expected. </span>
> <span style="color:#FF0000">Please change the shape of y to (n_samples,), for example using ravel(). return</span>
> <span style="color:#FF0000">fit_method(estimator, *args, **kwargs)</span>

```run-bash
# Explanation for these topics for 15 minutes approx
##  Split the Dataset for Training & Testing
##  Random Forest Classifier
##  Hyper-Parameter Optimization using GridSearchCV

mpv --start=01:29:20 --end=01:44:20 '/run/media/munish259272/WD5TB/Backups/learnbay files/machine learning/33. Class 64 , Apr 20, 2024, Sat ( Machine Learning).mp4' 2> /dev/null
```
> [!question]- [ChatGPT: Can you help me the above code ?](https://chat.openai.com/c/7bc83907-93be-4f26-bab0-8a905f16d36d#:~:text=Can%20you%20help%20me%20understand%20this%20%3F)
> Sure! This code is performing hyperparameter tuning for a Random Forest classifier using Grid Search with cross-validation. Let's break it down:
> 
> 1. **Random Forest Classifier Initialization**:
>     
>     ```
> 	fit_rf = RandomForestClassifier(random_state=42)
> 	```
>     
>     Here, a Random Forest classifier object is initialized with `random_state=42` to ensure reproducibility.
>     
> 2. **Setting Random Seed**:
>     
>     ```
> 	np.random.seed(42)
> 	```
>     
>     This sets the random seed to 42 for NumPy's random number generator. Setting a random seed ensures that the results are reproducible.
>     
> 3. **Defining Parameter Grid**:
>     
>     ```
> 	param_dist = {'max_depth': [2, 3, 4],
>               'bootstrap': [True, False],
>               'max_features': ['auto', 'sqrt', 'log2', None],
>               'criterion': ['gini', 'entropy']}
> 	```
>     
>     This dictionary `param_dist` defines the grid of hyperparameters to search over. It includes various values for `max_depth`, `bootstrap`, `max_features`, and `criterion`.
>     
> 4. **Initializing Grid Search Cross-Validation**:
>     
>     ```
> 	cv_rf = GridSearchCV(fit_rf, cv=10, param_grid=param_dist, n_jobs=3)
> 	```
>     
>     Here, a `GridSearchCV` object is initialized with:
>     
>     - `fit_rf`: The estimator (Random Forest classifier) to use for fitting the data.
>     - `cv=10`: Specifies 10-fold cross-validation.
>     - `param_grid=param_dist`: Specifies the dictionary of hyperparameters to search over.
>     - `n_jobs=3`: Number of jobs to run in parallel during grid search. `-1` indicates using all available processors.
> 5. **Performing Grid Search**:
>     
>     ```
> 	cv_rf.fit(training_set, class_set)
> 	```
>     
>     This line performs the grid search with cross-validation on the training data (`training_set`, `class_set`). It searches for the best combination of hyperparameters that maximizes the model's performance, as evaluated by the chosen scoring metric.
>     
> 6. **Printing Best Parameters and Execution Time**:
> 
>     ```
> 	print('Best Parameters using grid search: \n', cv_rf.best_params_) 
> 	end = time.time() 
> 	print('Time taken in grid search: {0: .2f}'.format(end - start))
> 	```
>     
>     After the grid search is completed, it prints out the best hyperparameters found (`cv_rf.best_params_`) and the time taken for the grid search.
>     
> 
> In summary, this code efficiently searches for the best hyperparameters for a Random Forest classifier using Grid Search with cross-validation, helping to optimize the model's performance.
> 
> > [!question]- [ChatGPT: Please help me understand the hyperparameters `bootstrap` and `max_features` ?](https://chat.openai.com/c/7bc83907-93be-4f26-bab0-8a905f16d36d#:~:text=what%20is%20bootstrap%20and%20what%20difference%20does%20it%20makes%20if%20it%20is%20set%20to%20True%20or%20False)
> > In the context of the Random Forest algorithm, the `bootstrap` parameter determines whether bootstrap samples are used when building trees. Let's break down both the `bootstrap` and `max_features` parameters:
> > 
> > 1. **bootstrap**:
> >     
> >     - When `bootstrap` is set to `True` (default), each tree in the Random Forest is built on a bootstrap sample of the training data. Bootstrap sampling involves randomly selecting samples from the training dataset with replacement.
> >     - When `bootstrap` is set to `False`, the entire training dataset is used to build each tree. This is known as using the whole dataset without bootstrap sampling.
> >     
> >     The difference between `True` and `False` lies in how the trees are constructed:
> >     
> >     - Using bootstrap samples (`bootstrap=True`) introduces randomness and diversity among the trees, which can help improve the robustness and generalization of the model.
> >     - Using the whole dataset (`bootstrap=False`) may reduce the randomness in the trees but can also lead to higher correlation among the trees, potentially resulting in overfitting.
> > 2. **max_features**:
> >     
> >     - `max_features` determines the maximum number of features to consider when looking for the best split at each node of the tree.
> >     - The parameter can take several values:
> >         - `'auto'`: This is equivalent to `sqrt(n_features)`, where `n_features` is the total number of features. It means that the algorithm will consider the square root of the total number of features at each split.
> >         - `'sqrt'`: Same as `'auto'`, equivalent to `sqrt(n_features)`.
> >         - `'log2'`: Equivalent to `log2(n_features)`.
> >         - `None`: This means that the algorithm will consider all features at each split.
> >     
> >     The choice of `max_features` influences the diversity of the trees in the Random Forest:
> >     
> >     - Using fewer features at each split can reduce overfitting by increasing the diversity among the trees.
> >     - Using more features can result in trees with higher predictive power but may also lead to overfitting.
> > 
> > In summary, `bootstrap` and `max_features` are parameters in the Random Forest algorithm that control how trees are constructed and can significantly impact the performance and behavior of the model.

> [!question]- [ChatGPT : What is the need for np.random.seed(42) ? i do not see it used anywhere other than top line ?](https://chat.openai.com/c/7bc83907-93be-4f26-bab0-8a905f16d36d#:~:text=What%20is%20the%20need%20for%20np.random.seed(42)%20%3F%20i%20do%20not%20see%20it%20used%20anywhere%20other%20than%20top%20line%20%3F)
> The line `np.random.seed(42)` sets the random seed for NumPy's random number generator to 42. Setting a random seed ensures reproducibility of random processes, such as generating random numbers or shuffling data.
> 
> In the provided code snippet, the random seed is set at the beginning, but it's not directly used in subsequent lines. However, it indirectly affects the behavior of functions or algorithms that rely on random numbers generated by NumPy, even if they are not explicitly mentioned.
> 
> In this case, the random seed ensures that any randomness introduced by NumPy's random number generator, such as the shuffling of data during cross-validation or the random initialization of parameters in the Random Forest model, will be consistent across different runs of the code. This is crucial for reproducibility, especially when conducting experiments or comparing results.
> 
> While it may seem unnecessary in this specific snippet, setting a random seed is a good practice in machine learning workflows to ensure that results are consistent and reproducible. It helps in debugging, sharing code with others, and documenting experiments.

```run-python
# Set best parameters given by grid search 
fit_rf.set_params(criterion = 'gini',
                  max_features = 'log2', 
                  max_depth = 3)
```

## OOB Rate
```run-python
fit_rf.set_params(warm_start=True, 
                  oob_score=True)

min_estimators = 15
max_estimators = 1000

error_rate = {}

for i in range(min_estimators, max_estimators + 1):
    fit_rf.set_params(n_estimators=i)
    fit_rf.fit(training_set, class_set)

    oob_error = 1 - fit_rf.oob_score_
    error_rate[i] = oob_error
```

```run-python
# Convert dictionary to a pandas series for easy plotting 
oob_series = pd.Series(error_rate)
```

```run-python
fig, ax = plt.subplots(figsize=(10, 10))

ax.set_facecolor('#fafafa')

oob_series.plot(kind='line',color = 'red')
plt.axhline(0.055, color='#875FDB',linestyle='--')
plt.axhline(0.05, color='#875FDB',linestyle='--')
plt.xlabel('n_estimators')
plt.ylabel('OOB Error Rate')
plt.title('OOB Error Rate Across various Forest sizes \n(From 15 to 1000 trees)')
```

```run-python
print('OOB Error rate for 400 trees is: {0:.5f}'.format(oob_series[400]))
```

```run-python
# Refine the tree via OOB Output
fit_rf.set_params(n_estimators=400,
                  bootstrap = True,
                  oob_score=False)
```

## Train the RandomForest
```run-python
fit_rf.fit(training_set, class_set)
```
> [!important]- [ChatGPT : `warm_start=True` may not be required after we have found the best parameters and `n_estimators` based on a stable **<span style="font-weight:bold; color:#000000">OOB Error rate</span>**](https://chat.openai.com/c/7bc83907-93be-4f26-bab0-8a905f16d36d#:~:text=I%20understand%20that%20the%20value%20of%20n_estimator%20was%20set%20to%201000%20when%20i%20ran%20the%20loop%20before%20so%20it%20is%20giving%20me%20the%20error%20and%20i%20also%20know%20that%20warm_start%3DFalse%20will%20fix)
> In your code, you are trying to train the RandomForestClassifier with `warm_start=True`, indicating that you want to perform incremental training, and you've set `n_estimators=400`. However, you encountered the error "ValueError: n_estimators=400 must be larger or equal to len(estimators_)=1000 when warm_start==True". This error occurs because `warm_start=True` expects `n_estimators` to be equal to or larger than the number of estimators already present in the model when you are fitting it.
> 
> To resolve this issue, you have a few options:
> 
> 1. **Increase `n_estimators`**: If you intend to use `warm_start=True`, you need to set `n_estimators` to a value equal to or larger than the number of estimators already present in the model. In your case, you encountered the error because the previous loop set `n_estimators` up to 1000. You can either set `n_estimators` to a value larger than 1000 or reset it to a lower value that you intend to use for further training.
>     
> 2. **Reset `n_estimators`**: If you don't need to retain the estimators from previous training iterations and you are not performing incremental training, you can reset `n_estimators` to a lower value or the desired value for your current training session. This would involve setting `warm_start=False`.
>     
> 3. **Do Not Use `warm_start=True`**: If you've found the best parameters and you're training the RandomForestClassifier from scratch without incremental training, you can set `warm_start=False`. This would avoid the error you encountered and allow you to train the model without considering previous estimators.
>     
> 
> The right solution depends on your specific requirements and whether you need to perform incremental training or not. If you don't need to perform incremental training or retain previous estimators, setting `warm_start=False` is a straightforward solution. Otherwise, you should adjust `n_estimators` accordingly to accommodate the existing estimators when using `warm_start=True`.

## Variable Importance
```run-python
def variable_importance(fit):
    """
    Purpose
    ----------
    Checks if model is fitted CART model then produces variable importance
    and respective indices in dictionary.

    Parameters
    ----------
    * fit:  Fitted model containing the attribute feature_importances_

    Returns
    ----------
    Dictionary containing arrays with importance score and index of columns
    ordered in descending order of importance.
    """
    try:
        if not hasattr(fit, 'fit'):
            return print("'{0}' is not an instantiated model from scikit-learn".format(fit)) 

        # Captures whether the model has been trained
        if not vars(fit)["estimators_"]:
            return print("Model does not appear to be trained.")
    except KeyError:
        print("Model entered does not contain 'estimators_' attribute.")

    importances = fit.feature_importances_
    indices = np.argsort(importances)[::-1]
    return {'importance': importances,
            'index': indices}
```

```run-python
var_imp_rf = variable_importance(fit_rf)

importances_rf = var_imp_rf['importance']

indices_rf = var_imp_rf['index']
```

```run-python
def print_var_importance(importance, indices, name_index):
    """
    Purpose
    ----------
    Prints dependent variable names ordered from largest to smallest
    based on information gain for CART model.
    Parameters
    ----------
    * importance: Array returned from feature_importances_ for CART
                models organized by dataframe index
    * indices: Organized index of dataframe from largest to smallest
                based on feature_importances_
    * name_index: Name of columns included in model

    Returns
    ----------
    Prints feature importance in descending order
    """
    print("Feature ranking:")

    for f in range(0, indices.shape[0]):
        i = f
        print("{0}. The feature '{1}' has a Mean Decrease in Impurity of {2:.5f}"
              .format(f + 1,
                      names_index[indices[i]],
                      importance[indices[f]]))
```

```run-python
print_var_importance(importances_rf, indices_rf, names_index)
```

## Predictions
```run-python
predictions_rf = fit_rf.predict(test_set)
```

## Confusion Matrix
```run-python
def create_conf_mat(test_class_set, predictions):
    """Function returns confusion matrix comparing two arrays"""
    if (len(test_class_set.shape) != len(predictions.shape) == 1):
        return print('Arrays entered are not 1-D.\nPlease enter the correctly sized sets.')
    elif (test_class_set.shape != predictions.shape):
        return print('Number of values inside the Arrays are not equal to each other.\nPlease make sure the array has the same number of instances.')
    else:
        # Set Metrics
        test_crosstb_comp = pd.crosstab(index = test_class_set,
                                        columns = predictions)
        # Changed for Future deprecation of as_matrix
        test_crosstb = test_crosstb_comp.values
        return test_crosstb
```

```run-python
conf_mat = create_conf_mat(test_class_set, predictions_rf)
sns.heatmap(conf_mat, annot=True, fmt='d', cbar=False)
plt.xlabel('Predicted Values')
plt.ylabel('Actual Values')
plt.title('Actual vs. Predicted Confusion Matrix')
plt.show()
```

## Accuracy
```run-python
accuracy_rf = fit_rf.score(test_set, test_class_set)

print("Here is our accuracy on the test set:\n {0:.3f}"\
      .format(accuracy_rf))
```

## Error Rate
```run-python
# Here we calculate the test error rate!
test_error_rate_rf = 1 - accuracy_rf
print("The test error rate for our model is:\n {0: .4f}"\
      .format(test_error_rate_rf))
```

## Area Under Curve (AUC)
```run-python
# We grab the second array from the output which corresponds to
# to the predicted probabilites of positive classes 
# Ordered wrt fit.classes_ in our case [0, 1] where 1 is our positive class
predictions_prob = fit_rf.predict_proba(test_set)[:, 1]

fpr2, tpr2, _ = roc_curve(test_class_set,
                          predictions_prob,
                          pos_label = 1)
```

```run-python
auc_rf = auc(fpr2, tpr2)
```

```run-python
def plot_roc_curve(fpr, tpr, auc, estimator, xlim=None, ylim=None):
    """
    Purpose
    ----------
    Function creates ROC Curve for respective model given selected parameters.
    Optional x and y limits to zoom into graph

    Parameters
    ----------
    * fpr: Array returned from sklearn.metrics.roc_curve for increasing
            false positive rates
    * tpr: Array returned from sklearn.metrics.roc_curve for increasing
            true positive rates
    * auc: Float returned from sklearn.metrics.auc (Area under Curve)
    * estimator: String represenation of appropriate model, can only contain the
    following: ['knn', 'rf', 'nn']
    * xlim: Set upper and lower x-limits
    * ylim: Set upper and lower y-limits
    """
    my_estimators = {'knn': ['Kth Nearest Neighbor', 'deeppink'],
              'rf': ['Random Forest', 'red'],
              'nn': ['Neural Network', 'purple']}

    try:
        plot_title = my_estimators[estimator][0]
        color_value = my_estimators[estimator][1]
    except KeyError as e:
        print("'{0}' does not correspond with the appropriate key inside the estimators dictionary. \
\nPlease refer to function to check `my_estimators` dictionary.".format(estimator))
        raise

    fig, ax = plt.subplots(figsize=(10, 10))
    ax.set_facecolor('#fafafa')

    plt.plot(fpr, tpr,
             color=color_value,
             linewidth=1)
    plt.title('ROC Curve For {0} (AUC = {1: 0.3f})'\
              .format(plot_title, auc))

    plt.plot([0, 1], [0, 1], 'k--', lw=2) # Add Diagonal line
    plt.plot([0, 0], [1, 0], 'k--', lw=2, color = 'black')
    plt.plot([1, 0], [1, 1], 'k--', lw=2, color = 'black')
    if xlim is not None:
        plt.xlim(*xlim)
    if ylim is not None:
        plt.ylim(*ylim)
    plt.xlabel('False Positive Rate')
    plt.ylabel('True Positive Rate')
    plt.show()
    plt.close()
```

```run-python
plot_roc_curve(fpr2, tpr2, auc_rf, 'rf',
               xlim=(-0.01, 1.05), 
               ylim=(0.001, 1.05))
```

## Classification Report
```run-python
def print_class_report(predictions, alg_name):
    """
    Purpose
    ----------
    Function helps automate the report generated by the
    sklearn package. Useful for multiple model comparison

    Parameters:
    ----------
    predictions: The predictions made by the algorithm used
    alg_name: String containing the name of the algorithm used
    
    Returns:
    ----------
    Returns classification report generated from sklearn. 
    """
    print('Classification Report for {0}:'.format(alg_name))
    print(classification_report(predictions, 
            test_class_set, 
            target_names = dx))
```

```run-python
class_report = print_class_report(predictions_rf, 'Random Forest')
```

```run-python
#Assignmnt - try to do Adaboost Regressor by using it on insurance data

# Assuming that X_train, X_test, y_train and y_test is available and we have already have a base estimator = DecisionTree

from sklearn.ensemble import AdaBoostClassifier

#Base estimator (now it is renamed as estimator), by default if we do not provide the base estimator, then it will select DT with max_depth = 1.
base_classifier = DecisionTreeClassifier(max_depth = 2)

#Instanciate the classifier
adaboost_classifier = AdaBoostClassifier(estimator = base_classifier, n_estimators = 30, random_state = 37)

#Fit the data
adaboost_classifier.fit(X_train, y_train)

#Predict (Training)

ada_train_pred = adaboost_classifier.predict(X_train)


#Predict (Test)

ada_test_pred = adaboost_classifier.predict(X_test)


#Findout accuracy, precision, recall, f1-score etc.
```

# At 02:33:30 K-nearest neighbors
[KNN Final.pdf](/img/user/Learning/Programming/C&T/Learnbay/Statistics%20&%20Machine%20Learning/course%20files/Machine%20Learning%20with%20Dhruv-%2016th%20March%202024-20240410T173353Z-001/Machine%20Learning%20with%20Dhruv-%2016th%20March%202024/7.KNN/KNN%20Final.pdf)
```run-python
play_video("02:33:30", "02:33:30")
```
![33. Class 64 , Apr 20, 2024, Sat ( Machine Learning)-20240524083602334.png](/img/user/attachments/33.%20Class%2064%20,%20Apr%2020,%202024,%20Sat%20(%20Machine%20Learning)-20240524083602334.png)

## <span style="color:#000000;font-size:1.5em"><i>---Continued to next class ---&gt</i></span>

## <div style='text-align:center'><hr/><h2>OBSIDIANTOANKI ANKIBRIDGE</h2><hr/></div>

TARGET DECK: ML&AI :: ML
FILE TAGS: ML::DecisionTree

<!---->
33.Class 64 , Apr 20, 2024, Sat ( Machine Learning) : In classification we choose max impurity decrease as a choice for splitting then how will you choose the variable for splitting in case of  regression? #flashcard 
Answer : The split will happen based on the variable which will reduce the MSE/variance most 
<!--ID: 1716866943854-->



<!---->
33.Class 64 , Apr 20, 2024, Sat ( Machine Learning) : How classification is done in K-Nearest neighbors ? #flashcard 
If we select k nearest data points to a new data point then new data point is assigned the majority class of k nearest data points
![33. Class 64 , Apr 20, 2024, Sat ( Machine Learning)-20240524084143360.png](/img/user/attachments/33.%20Class%2064%20,%20Apr%2020,%202024,%20Sat%20(%20Machine%20Learning)-20240524084143360.png)
![33. Class 64 , Apr 20, 2024, Sat ( Machine Learning)-20240524084210664.png](/img/user/attachments/33.%20Class%2064%20,%20Apr%2020,%202024,%20Sat%20(%20Machine%20Learning)-20240524084210664.png)
<!--ID: 1716866943874-->



<!---->
33.Class 64 , Apr 20, 2024, Sat ( Machine Learning) : Given
```run-python

data = pd.read_csv(present_working_directory / 'course files/Machine Learning with Dhruv- 16th March 2024-20240410T173353Z-001/Machine Learning with Dhruv- 16th March 2024//4.Logistic Regression/Data+Code/bank.csv')
```
import the required libraries and implement decision tree(DT) using sklearn on the given data and do all the evaluation metrics.
Use the link [here](obsidian://open?vault=Programming_vault&file=Programming%2FCourses%20and%20Tutorials%2FLearnbay%2FStatistics%20%26%20Machine%20Learning%2FTest%20run%2FTest%20run%20for%20DT%2033.%20Class%2064%20%2C%20Apr%2020%2C%202024%2C%20Sat) to write the code, test and run and mark the answer accordingly and proceed. 
Step 1. What are the libraries to import for reading file, EDA, training and testing the model and Evaluation of the model? 
Step 2. Proceed ? #flashcard 
### imports and file read
```run-python
import pandas as pd
import numpy as np
from sklearn import preprocessing
from sklearn.model_selection import train_test_split
from sklearn.tree import DecisionTreeClassifier
from sklearn.metrics import confusion_matrix
from sklearn.metrics import accuracy_score
from sklearn.metrics import roc_curve, auc
import matplotlib.pyplot as plt
 
# from sklearn.externals.six import StringIO  
from IPython.display import Image         # To show the tree based structure
from sklearn.tree import export_graphviz  # To show the tree
import pydotplus                          # To show the tree
import seaborn as sns
```
<!--ID: 1716866943894-->



<!---->
33.Class 64 , Apr 20, 2024, Sat ( Machine Learning) : Given
```run-python

data = pd.read_csv(present_working_directory / 'course files/Machine Learning with Dhruv- 16th March 2024-20240410T173353Z-001/Machine Learning with Dhruv- 16th March 2024/6.Bagging & Boosting/Code+Dataset/data.csv')
```
import the required libraries and implement decision tree(RF) using sklearn on the given data and do all the evaluation metrics.
Use the link [here](obsidian://open?vault=Programming_vault&file=Programming%2FCourses%20and%20Tutorials%2FLearnbay%2FStatistics%20%26%20Machine%20Learning%2FTest%20run%2FTest%20run%20for%20RF%2033.%20Class%2064%20%2C%20Apr%2020%2C%202024%2C%20Sat) to write the code, test and run and mark the answer accordingly and proceed.
Step 1. What are the libraries to import for reading file, EDA, training and testing the model and Evaluation of the model? 
Step 2. Proceed ? #flashcard 
### imports and file read
```run-python
import time
import random
import numpy as np
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt
from sklearn.metrics import roc_curve, auc
from sklearn.metrics import confusion_matrix
from sklearn.metrics import classification_report
from sklearn.model_selection import KFold, cross_val_score
from sklearn.model_selection import train_test_split, GridSearchCV
from sklearn.ensemble import RandomForestClassifier 
from urllib.request import urlopen 
 
plt.style.use('ggplot')
pd.set_option('display.max_columns', 500) 
```
<!--ID: 1716866943917-->



<!---->
33.Class 64 , Apr 20, 2024, Sat ( Machine Learning) : How do i remember the steps for importing different models or do i need to remember them ? Can you suggest anything like mnemonics to remember or steps to proceed in a model training , testing , evaluation ? #flashcard 
> [!question]- ChatGPT : How to i remember the steps for importing different models or do i need to remember them ? Can you suggest anything like mnemonics to remember or steps to proceed in a model training , testing , evaluation ?
> Remembering the steps for importing different models and proceeding with model training, testing, and evaluation can be simplified by understanding the general workflow and using a structured approach. Here's a breakdown that can help you:
> 
> ### General Workflow for Model Training and Evaluation
> 
> 1. **Data Loading and Exploration**
>     
>     - Load the data using `pandas`.
>     - Explore the data using `pandas` and `seaborn` for visualization.
> 2. **Data Preprocessing**
>     
>     - Handle missing values, if any.
>     - Encode categorical variables if necessary (`LabelEncoder` or `pd.get_dummies`).
>     - Standardize/normalize the data if required (`StandardScaler`).
> 3. **Data Splitting**
>     
>     - Split the data into training and testing sets using `train_test_split`.
> 4. **Model Training**
>     
>     - Import the model.
>     - Instantiate the model.
>     - Train the model using the training data.
> 5. **Model Evaluation**
>     
>     - Make predictions on the test data.
>     - Evaluate the model using metrics like accuracy, confusion matrix, classification report, ROC curve, etc.
> 
> ### Mnemonics and Tips
> 
> To remember the steps and imports, you can use mnemonics or acronyms. Here's an example:
> 
> **DEPS - D.M.E.T.E.**
> 
> 1. **D**ata Loading and Exploration
> 2. **E**ncoding and Preprocessing
> 3. **P**artitioning (Splitting the Data)
> 4. **S**tandardizing and Scaling (if necessary)
> 
> **M - T.R.I.E.**
> 
> 1. **M**odel Training
> 2. **T**esting
> 3. **R**eporting Metrics
> 4. **I**mprovement and Hyperparameter Tuning
> 5. **E**valuation
> 
> ### Example with Mnemonics
> 
> Let's apply this structure to the K-Nearest Neighbors (KNN) model:
> 
> **DEPS**
> 
> 1. **D**ata Loading and Exploration
> ```run-python
> import pandas as pd
> import seaborn as sns
> import matplotlib.pyplot as plt
> 
> data = pd.read_csv('your_data.csv')
> sns.pairplot(data)
> plt.show()
> ```
>     
> 2. **E**ncoding and Preprocessing
> ```run-python
> from sklearn.preprocessing import StandardScaler
> scaler = StandardScaler()
> data_scaled = scaler.fit_transform(data)
> ```
>     
> 3. **P**artitioning
> ```run-python
> from sklearn.model_selection import train_test_split
> X_train, X_test, y_train, y_test = train_test_split(data_scaled, target, test_size=0.2, random_state=42)
> ```
>     
> 
> **M**
> 
> 1. **T**raining
> ```run-python
> from sklearn.neighbors import KNeighborsClassifier
> knn = KNeighborsClassifier(n_neighbors=5)
> knn.fit(X_train, y_train)
> ```
>     
> 2. **R**eporting Metrics
> ```run-python
> from sklearn.metrics import classification_report, confusion_matrix
> y_pred = knn.predict(X_test)
> 
> print(confusion_matrix(y_test, y_pred))
> print(classification_report(y_test, y_pred))
> ```
>     
> 
> ### Summary Table for Imports
> 
> |Step|KNN|Logistic Regression|Decision Tree|Random Forest|
> |---|---|---|---|---|
> |Data Loading|`import pandas as pd`|`import pandas as pd`|`import pandas as pd`|`import pandas as pd`|
> |Exploration|`import seaborn as sns`|`import seaborn as sns`|`import seaborn as sns`|`import seaborn as sns`|
> |Preprocessing|`from sklearn.preprocessing import StandardScaler`|`from sklearn.preprocessing import StandardScaler, LabelEncoder`|`from sklearn import preprocessing`|`from sklearn.preprocessing import StandardScaler`|
> |Train-Test Split|`from sklearn.model_selection import train_test_split`|`from sklearn.model_selection import train_test_split`|`from sklearn.model_selection import train_test_split`|`from sklearn.model_selection import train_test_split`|
> |Model Import|`from sklearn.neighbors import KNeighborsClassifier`|`from sklearn.linear_model import LogisticRegression`|`from sklearn.tree import DecisionTreeClassifier`|`from sklearn.ensemble import RandomForestClassifier`|
> |Metrics|`from sklearn.metrics import classification_report, confusion_matrix`|`from sklearn.metrics import accuracy_score, confusion_matrix, classification_report, roc_curve, roc_auc_score`|`from sklearn.metrics import accuracy_score, confusion_matrix, roc_curve, auc`|`from sklearn.metrics import classification_report, confusion_matrix, roc_curve, auc`|
> 
> ### Practice and Consistency
> 
> The key to remembering these steps and imports is consistent practice. Over time, the repetitive nature of the tasks will help reinforce your memory. Additionally, referring to this structured approach whenever you start a new project will build a strong foundation.
<!--ID: 1716866943934-->




